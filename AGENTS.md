# 项目固定开发与 GitHub 版本管理流程

用户要求：每次完成代码更新后，执行验证、Git 提交、GitHub 推送并报告版本。此约定适用于本仓库后续开发任务；除用户明确要求暂停外，不需要每次再次请求提交或推送许可。

## 开始开发

- 先检查分支、远程、工作区和暂存区，识别当前任务与已有改动的边界。
- 使用 `codex/<task-name>` 开发分支。一个连续任务可复用其分支；新任务创建独立分支。不得默认直接向 main 推送或自动合并。
- 工作区已有改动属于用户；不得通过 reset、clean、强制 checkout 或批量暂存吞并其他任务的修改。无法安全分离重叠改动时，说明具体问题。

## 实现与验证

- 共享算法权威源是 `sa_cpp/`。遵循 `docs/REPOSITORY_GUIDE.md` 的同步要求，运行 `scripts/sync_scene_engine_to_sa.ps1` 及 `-Check`。
- 按改动影响运行必要构建和测试。改变 HSMM、模板或推送行为时，用可用的固定真实数据回放比较误推、漏报与推送时间；说明数据范围和验证限制。
- 更新受影响文档，区分已实现能力、已验证结果与待实现设计。

## 每次代码更新完成后

1. 检查 diff 和 `git diff --check`，逐文件暂存本次相关源码、测试、必要配置及文档。
2. 检查暂存差异。不得上传 API key、agent.env、凭据、个人原始传感器数据、精确个人位置、未经脱敏的日志、缓存或构建产物。不能仅依赖 .gitignore，因为已跟踪文件仍可含敏感数据。
3. 创建语义明确的 commit；正文记录重要行为变化及验证结果。一个独立功能或修复保持为可识别的提交，不默认 amend 已推送历史。
4. 推送到已核验的 GitHub origin 开发分支，首次推送设置 upstream。不得 force push、删除远程分支或自动合并 main。
5. 核验 GitHub 远程分支 SHA 与本地提交一致。推送失败时保留本地提交，报告具体阻碍；不得宣称已完成云端备份。
6. 最终回复包含：变更摘要、验证结果、分支名、commit 短 SHA 和 GitHub commit 链接，以及仍未提交的相关工作（如有）。

## 版本与回退

- 每次完成的开发以 commit 为版本标识；不为每次小改动自动创建 release 或 tag。
- 用户明确要求里程碑版本时再建立版本 tag / release。
- 回退优先使用可审计的 revert，不改写共享历史。
- 纯解释或只读分析任务不需要制造空提交。

## 历史工作区

此流程建立时仓库已有大量未提交修改。不得将它们作为一次新任务的附带内容上传；应在明确整理历史版本的任务中，检查源码、敏感配置和实验文件后单独形成版本。
