# Agent 模板功能同步版本（2026-09-07）

对照远程 main `0261598`，本版本保存此前已落地但未提交的功能，不实现后续讨论中的主动取证或新风险模型。

## 本版本包含

- 主机 Jiuwen Agent：读取证据、真实锚点、受支持原语目录，组合正向序列与负向模式；正常工具权限禁止直接修改 theta。
- C++ 模板工具：结构校验、LOW/MEDIUM/HIGH 强度试验、历史回放、提交守卫、模板保存与实时加载。
- 个人参数族：垂直阈值由确定性代码估计，departure_time 新申请保持关闭。
- HSMM 阶段1/2：原子证据退出动态转移公式；模板输出独立序列字段，进度与作用强度分离；历史回放重建模板证据。
- Wi-Fi 相似度、脱离/重连及零权重处理改进；默认 BLE 权重关闭。
- 历史归因/规则优化器及离线对照脚本，作为实验依赖保留；普通主机 Agent 不开放这些数值修改工具。
- 对应测试、Python 镜像、OHOS 共享源码副本和文档。

## 验证与已知限制

全新 WSL CMake 构建完成；HSMM、场景、无线、PDR、气压、gapfill、证据查询、action、个性化优化器和离线工具共10项测试通过。Jiuwen 主机 personalizer 编译链接成功；本次变更的 Python 文件语法检查、工具契约 JSON 解析、共享源码同步检查通过。

固定历史窗口回放：0812 为5条episode，基线与冻结序列模板score均为8；0813为12条，均为16.5。两组冻结模板均无硬误推、无漏报（沿用当前低层平台软正例评分口径）。本次未调用在线LLM，不代表重新验证了Agent输出质量。

推送偏晚仍是已知问题；现有score对提前量变化不够敏感。此提交保存可复现版本，不声称解决了提前量或验证了跨人泛化。详情见 `STAGE1_STAGE2_ABLATION.md`。

主机 Agent 已切换为模板模式。`sa_service` 是参考SA实现，其 ProactiveAgent Prompt仍使用旧受约束数值优化器流程，不能据共享源码同步宣称设备Agent权限也已迁移。本仓库README指定的真机宿主为另一个工程 `D:\helloworld_agent`，本次没有修改或部署该工程。

本次不上传个人无线指纹更新、原始采集数据、运行日志、API密钥、构建产物、POI测试包和未整理的专利草稿。真实回放需要本地数据；干净克隆可运行合成主机测试。远程已有的历史个人配置没有在本次改写或清理，部署时应使用自己的锚点和无线配置。

## 后续开发边界

主动取证、AND/OR/HOLD/NOT_WITHIN通用表达式、早期前缀校准与改进提前量评分均为计划，不属于此版本。
